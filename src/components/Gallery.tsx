import React from 'react';
import { StyledGallery } from './styles/Gallery.styled';

type GalleryProps = {
  img: string[];
};

export default function Gallery(): JSX.Element {
  return <StyledGallery>hallos</StyledGallery>;
}
